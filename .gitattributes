# Normalize all text files to CRLF in the working tree, regardless of
# what line endings get written to them. Stored as LF in the repo.
* text=auto eol=crlf

# Binary assets, never touch these
*.png binary
*.tga binary
*.otf binary
*.ttf binary
