local L1UI, E, L, V, P, G = unpack(select(2, ...))

-- Defaults: E.private.L1UI
V.L1UI = {
	disabledFrames = {
		AlertFrame = false,
		BossBanner = false,
		ZoneTextFrame = false,
	},
	qualityOfLife = {
		easyDelete = false,
	},
	skins = {
		BugSack = false,
	}
}
