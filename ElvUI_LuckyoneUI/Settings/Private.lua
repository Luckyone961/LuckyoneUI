local L1UI, E, L, V, P, G = unpack(select(2, ...))

-- LuckyoneUI Private DB

V.L1UI = {

	disabledFrames = {
		AlertFrame = false,
		BossBanner = false,
		ZoneTextFrame = false,
	},

	qualityOfLife = {
		easyDelete = false,
	},

}
