local L1UI, E, L, V, P, G = unpack(select(2, ...))

-- Defaults: E.private.L1UI
V.disabledFrames = {
	AlertFrame = false,
	BossBanner = false,
	ZoneTextFrame = false,
}

V.qualityOfLife = {
	easyDelete = false,
}

V.skins = {
	BugSack = false,
}
