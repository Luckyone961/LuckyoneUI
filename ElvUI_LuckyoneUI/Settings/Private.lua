local L1UI, E, L, V, P, G = unpack(select(2, ...))

-- LuckyoneUI Private DB

V.L1UI = {

	disabledFrames = {
		AlertFrame = false,
		BossBanner = E.Retail and false or nil,
		ZoneTextFrame = false,
	},

}
