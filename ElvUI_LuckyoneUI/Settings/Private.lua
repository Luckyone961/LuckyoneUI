local L1UI, E, L, V, P, G = unpack(select(2, ...))

-- LuckyoneUI Private DB

V.L1UI = {

	install_version = nil,

	disabledFrames = {
		BossBanner = false,
		LevelUpDisplay = false,
		ZoneTextFrame = false,
		AlertFrame = false,
	},

	skins = {},

}
