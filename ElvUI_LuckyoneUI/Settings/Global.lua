local L1UI, E, L, V, P, G = unpack(select(2, ...))

-- LuckyoneUI Global DB

if E.Retail then

	G.L1UI = {}

elseif E.Classic then

	G.L1UI = {}

elseif E.TBC then

	G.L1UI = {}

end
