local L1UI, E, L, V, P, G = unpack(select(2, ...))

-- LuckyoneUI Profile DB

if E.Retail then

	P.L1UI = {}

elseif E.Classic then

	P.L1UI = {}

elseif E.TBC then

	P.L1UI = {}

end
